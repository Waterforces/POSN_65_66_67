#include<bits/stdc++.h>
#define endl '\n'
#define ll long long
using namespace std;
const int N = 1005;
int Q;
bool dp[N][N];
string a,b;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    cin >> a >> b >> Q;
    while(Q--) {
        memset(dp,0,sizeof(dp));
        string s;
        cin >> s;
        for(int i=0;i<=a.size();i++) {
            for(int j=0;j<=b.size();j++) {
                
            }
        }
    }
}